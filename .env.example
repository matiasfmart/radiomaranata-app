# URL of the privacy-preserving listener aggregation endpoint.
# It accepts POST { sessionId } and returns aggregated country/city counts.
EXPO_PUBLIC_LISTENERS_API_URL=https://your-listeners-api.example.com/api/listeners
