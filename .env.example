# Optional deployment environment label: development, staging, or production.
EXPO_PUBLIC_APP_ENV=

# Optional privacy-preserving listener aggregation endpoint.
# It accepts POST { sessionId } and returns aggregated country/city counts.
# Leave blank to disable listener location analytics.
EXPO_PUBLIC_LISTENERS_API_URL=
